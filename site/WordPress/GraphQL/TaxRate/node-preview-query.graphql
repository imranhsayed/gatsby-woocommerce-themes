query PREVIEW_QUERY($id: ID!) {
  taxRate(id: $id, idType: ID, asPreview: true) {
    city
    compound
    country
    id
    name
    order
    postcode
    priority
    rate
    rateId
    shipping
    state
    __typename
  }
}
