query PREVIEW_QUERY($id: ID!) {
  shippingClass(id: $id, idType: ID, asPreview: true) {
    count
    databaseId
    description
    id
    link
    name
    slug
    taxonomy {
      node {
        id
      }
    }
    termGroupId
    termTaxonomyId
    uri
    __typename
  }
}
